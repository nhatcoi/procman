# Example procman configuration
processes:
  api:
    cmd: "go run ./cmd/server"
    cwd: "."
    port: 8080
    watch: true   # Auto-restarts api when Go files in project change
    watch_ignore:
      - "tmp"
      - "*.sqlite"
    env:
      PORT: "8080"
      ENV: "development"

  web:
    cmd: "npm run dev"
    cwd: "./web"
    port: 3000
    depends_on:
      - api       # Waits for API port 8080 to open before starting web
    forward: true # Automatically starts Cloudflare quick tunnel when started

  worker:
    cmd: "python -m worker.main"
    cwd: "."
    depends_on:
      - api
    watch:
      - "worker"  # Watches only the worker directory
    env:
      WORKER_COUNT: "2"
